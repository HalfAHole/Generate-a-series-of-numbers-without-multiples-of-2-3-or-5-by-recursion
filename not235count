#include <stdio.h>
int main(void) {
long long int i=0, j=0, k=0, x=5, xx=5, a=0, n=1000, q=5, w=0, v=0, isprime=0, np=3, z=0, zz=0, qq=5;
long double ii;
printf("Counting odd numbers without multiples of 3 or 5.\n");

for (j=7; j<=n; j=j+3) {x=fact(j); if (x>n) {break;} z++; if (z!=7 && z!=10) {

printf("\nx=%d ", x);

}if (z==10) {z=0;}}}


fact(long long int n) {
	if (n<0) {return(-n);}
	else if (n==0 || n==1) {return(n);}
	else {return((2*n)-fact(n-1));}}
